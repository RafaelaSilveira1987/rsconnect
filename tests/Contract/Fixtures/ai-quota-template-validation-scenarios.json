{
  "version": "36.6.10",
  "scenarios": [
    {
      "name": "quick_queue_template_identity",
      "expect": ["file template-fila-rapida-ia.json exists", "schedule interval is 1 minute", "HTTP method is POST", "endpoint is /webhooks/ai-reprocess/queue", "header X-RS-AI-Reprocess-Token is present"]
    },
    {
      "name": "automatic_queue_respects_cooldown",
      "expect": ["queue_cron source does not set bypass_cooldown", "only source manual can bypass cooldown", "ai.cooldown remains pending until interval expires"]
    },
    {
      "name": "plan_limit_legacy_fallback",
      "expect": ["limitRows uses ai_interactions_month", "when absent it falls back to messages_month then ai_replies_month", "migration 053 repairs standard plan values"]
    }
  ]
}
